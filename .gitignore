.project/.loop_prompt.txt
.project/run_report.md
node_modules/

# 打包產物（dev/build_dist.sh 產生，不進版控）
dist/
*.zip

# 公司內部規範文件（標示「請勿外流」，絕不上公開 GitHub）
Cathay規範/
合規落差對照表.md

# 真實弱點資料檔（絕不進版控／不上 GitHub）
*.xlsx

# 官方 logo（商標，不上公開 GitHub；各機本機放置於 assets/，會優先於內建 logo.svg）
assets/logo.png
assets/logo.jpg

# 個人本機設定（不上 GitHub）
.claude/settings.local.json

# NB 端推送工具，不推上 GitHub（公司端只用 update.bat，避免混淆）
upload.bat

# Email 執行期產物／個人補漏（含實 email/relay IP，絕不上 GitHub）
override.json
dept_manager.json
mail-batch.json
mail-task.json
mail_log.csv

# update.bat 自我更新的暫存
update.bat.new

# 交接摘要（內部工作脈絡，留本機不上公開 repo）
交接摘要_*.md

# 小幫手存取權杖（每次啟動重產，絕不進版控）
agent_token.txt

# 測試報告（含安全細節與內部服務名，留本機不上公開 repo）
測試報告_*.md

# 對主管的資安改善報告（內部，留本機）
資安改善報告_*.md

# 待刪暫存區（整理搬出的舊檔，確認無用後可整個刪除）
_tmp_del/

# 方法論分享包（與本 app 無關，已移出至 C:\AiProject\）
一次性開發包*/
